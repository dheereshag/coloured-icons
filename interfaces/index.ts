export interface Icon {
  name: string;
  category: string;
  classes: string[];
  url: string;
}

export interface Category {
  name: string;
}
export interface Icon {
  name: string;
  classes: string[];
  url: string;
}
